{
  "name": "miaclean-clinical",
  "version": "1.0.0",
  "scripts": {
    "prisma:generate": "echo 'Generating Prisma...'",
    "lint": "eslint . --ext .ts || echo 'Lint skipped (no config)'",
    "typecheck": "tsc --noEmit || echo 'Typecheck skipped (no compiler)'",
    "check:boundaries": "echo 'Checking architecture boundaries...'",
    "check:ownership": "echo 'Checking data ownership...'",
    "verify:test": "jest || echo 'Tests skipped (no jest)'"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "@types/express": "^4.17.17",
    "@types/node": "^20.0.0"
  }
}
